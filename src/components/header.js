import React from 'react';
import { Text, AppRegistry } from 'react-native';
import Header from './src/components/header';

const Header = () =>(
    <Header />
);

// AppRegistry.registerComponent('forms' () => App );